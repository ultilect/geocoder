spring:
  datasource:
    url: jdbc:h2:mem:geocoder
    username: geocoder
    password: geocoder
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
