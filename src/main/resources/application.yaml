spring:
  datasource:
    url: jdbc:postgresql://geocoder-postgres:5432/geocoder
    username: geocoder
    password: geocoder
    driverClassName: org.postgresql.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.PostrgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
